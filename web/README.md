# Dashboard (placeholder)

The Next.js dashboard arrives in milestone **M5** (see `docs/PLAN.md` §15/§19):
market overview, symbol detail with zone bands drawn on a lightweight-charts
price chart, and the dark-pool tape — all with plain-language tooltips.

Until then the backend's FastAPI service is the only UI surface
(`GET /healthz` for now).
