# Alembic configuration. The database URL is intentionally NOT set here —
# it is read from the DATABASE_URL environment variable in alembic/env.py,
# so no credentials ever live in a committed file.

[alembic]
script_location = alembic
prepend_sys_path = .
